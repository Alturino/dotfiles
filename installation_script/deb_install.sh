#!/bin/bash
# NOTE: This script is superseded by ansible/ubuntu.yml
# For new installations, use:
#   cd ansible && ansible-playbook ubuntu.yml -i inventory.ini --tags "all"
# This script is kept for reference only.
